import { useDispatch } from "react-redux";

export function Task({ task }) {
  return (
    <>
      {/* <li>
        {task.name}{" "}
        <span
          role="button"
          onClick={() => {
            dispatch(actions.removeTask());
          }}
        >
          &times;
        </span>
      </li> */}
    </>
  );
}
